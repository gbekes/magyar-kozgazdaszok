# magyar-kozgazdaszok — Evidence for Hungary

A searchable, policy-facing catalogue of research by top Hungarian economists.
Static site; no framework, no build step for the pages themselves. See the live
site at (domain TBD).

## Quick start (local)

```bash
python build.py            # regenerates data/index.json
python -m http.server 8080 # serves the site
```

Open <http://localhost:8080/>. Hungarian mirror at `/hu/`.

## What's in this repo

```
/                       HTML pages (index, papers, paper, authors, author, topics, topic, about, contribute)
  hu/                   Hungarian-language mirror (currently landing only)
  assets/               style.css, app.js, chrome.js — all client-side, ~500 lines total
  data/
    papers/<slug>.json  one file per paper
    authors/<slug>.json one file per author
    topics.json, journals.json
    index.json          aggregated — regenerated by build.py
  scripts/
    ingest.py           three-stage content pipeline (OpenAlex → Crossref → Claude)
    apply_drafts.py     apply pre-generated drafts to paper files
    requirements.txt
  build.py              expand authors from seed + regenerate data/index.json
```

Docs:

- [**VISION.md**](docs/development/VISION.md) — what and why (audience, scope, open questions)
- [**SPEC.md**](SPEC.md) — inclusion criteria, data schemas, tech stack, site UX
- [**WORKFLOW.md**](docs/development/WORKFLOW.md) — how content gets from a name to a paper page
- [**TAXONOMY.md**](docs/development/TAXONOMY.md) — the controlled vocabularies (topics, methods, data types)
- [**SEED_ECONOMISTS.md**](docs/development/SEED_ECONOMISTS.md) — starter roster with sources
- [**BUILD.md**](docs/development/BUILD.md) — how to run locally and how to use the ingestion pipeline

## Adding a paper

1. Copy `sample-paper.json` to `data/papers/<slug>.json`.
2. Edit the fields. Slug format: `<lead-surname>[-<second-surname>|-et-al]-<year>-<short-outlet>`.
3. `python build.py`
4. Refresh the browser.

## Adding an author

1. Create `data/authors/<slug>.json` (schema in `SPEC.md` § 2.2).
2. Add the slug to `authors-seed.json` (a flat manifest of approved IDs).
3. `python build.py`

## Ingesting at scale

```bash
python -m pip install -r scripts/requirements.txt
export ANTHROPIC_API_KEY=sk-ant-...   # or set via your shell
python scripts/ingest.py run <author_id> --max-works 10
python build.py
```

Full workflow and safety guarantees are described in [BUILD.md](docs/development/BUILD.md).

## Deploy

Any static host.

### GitHub Pages (one-click)

This repo includes `.github/workflows/pages.yml` which publishes the whole
site on every push to `main`. Enable Pages in the repo settings →
Pages → Source = "GitHub Actions".

**Custom domain:** once you've bought a domain, add a file at the repo root
named `CNAME` containing just the domain (e.g., `evidence-hu.org`), push, and
configure the DNS at the registrar as GitHub Pages instructs (usually an A or
CNAME record pointing at `username.github.io`).

### Vercel / Netlify / Cloudflare Pages

Point at the repo root, no build command, output directory `.`. `build.py` can
be run as a build step if you want `data/index.json` refreshed on deploy:

```
python build.py
```

## Review and editorial

All AI-generated content is marked `review_status: ai-drafted`. After a human
editor checks and edits, flip to `human-reviewed`. If the author signs off, flip
to `author-approved`.

Corrections welcome: issues or PRs. Editor: Gábor Békés
(<bekesg@ceu.edu>).

## License

[**CC BY-NC-SA 4.0**](https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en)
— Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International.
Both the catalogue content (paper / author / topic JSON, summaries, bios)
and the site code are released under this licence. See [LICENSE](LICENSE)
for the full terms. Attribution to *Magyar Közgazdászok — Evidence for
Hungary* (<https://github.com/gbekes/magyar-kozgazdaszok>); commercial
use requires permission.
