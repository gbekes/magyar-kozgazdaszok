# Handover — 2026-04-23

One-session pickup note for a fresh Claude. Read `CLAUDE.md` first, then this.

## One-paragraph project summary

**Magyar Közgazdászok** — static site at `gbekes.github.io/magyar-kozgazdaszok/`
cataloguing top papers by Hungarian economists, aimed at policymakers. HTML +
vanilla JS + CSS, JSON data layer, GitHub Pages deploy on push to `main`.
Editor-in-chief: Gábor Békés (`bekesg@ceu.edu`). English site at root; Hungarian
mirror at `hu/`. Content pipeline: OpenAlex/RePEc → Crossref → editor + Claude
drafts. Scale right now: **82 authors, 713 papers, 19 topics**.

## Current HEAD + git state

- HEAD (pushed): `b9ddb58` — *"Add 6 strong-candidate authors + edit
  landing-page banner"*
- Working tree has 4 uncommitted author files ready to commit (see below).
- Recent commits:
  ```
  b9ddb58 Add 6 strong-candidate authors + edit landing-page banner
  33f9a4f Affiliation review: drop HUN-REN + fix Bencsik/Wachs/Lengyel/Juhász R.
  1eecef6 Add 4 unpublished 2023+ CEPR/NBER/CESifo working papers
  fde54d7 Standardize all 76 authors' affiliations (EN + HU)
  500ebf1 Kóczy feedback: bio update + dedupe 15 slug-duplicates + Omega + book
  ```

## Uncommitted changes (to commit next)

```
 M data/authors/barany-zsofia.json     → moved Sciences Po → CEU
 M data/authors/vonyo-tamas.json       → moved IMT Lucca → Bocconi
 M data/authors/szerb-laszlo.json      → marked deceased (1955–2024)
 M data/authors/kezdi-gabor.json       → marked deceased (1970–2023)
```

Not yet committed because they were editor corrections arriving at the very
end of the session. Commit first thing in the next session with:

```
git add data/authors/{barany-zsofia,vonyo-tamas,szerb-laszlo,kezdi-gabor}.json
git commit -m "Fix 4 authors: Bárány→CEU, Vonyó→Bocconi, Szerb/Kézdi deceased"
git push
```

## Catalogue state at a glance

| Metric | Value |
|---|---|
| Authors (total) | 82 |
| Authors marked deceased | 3 (Kornai, Kézdi, Szerb) |
| Papers (total) | 713 |
| — `article` | 607 |
| — `chapter` | 99 |
| — `working_paper` | 7 (all 2023+ CEPR / NBER / CESifo) |
| Papers at `review_status: ai-drafted` | 313 |
| Papers at `review_status: metadata-fetched` | 400 (no draft yet) |
| Papers with Hungarian translation | 144 of 313 drafted |
| Authors with `repec_id` set | 13 of 82 |
| Topics | 19 |

## Open TODOs

### High-priority editorial

1. **Drafts for the remaining ~400 `metadata-fetched` papers.** Most are
   Tier-B-or-below by authors with already-many drafted papers. Prioritise
   by Tier A / field-top. Use `scripts/apply_drafts.py <file.json>` — the
   file format is documented at the top of the script. Template for each
   paper: `summary_en` (80–150 words, policy-facing), `data_used` (40–80),
   `policy_relevance` (60–120 with Hungarian hook + caveat), tags
   (topics/methods/data_types/countries/policy_instruments). 170 papers
   also still need `summary_hu` / `data_used_hu` / `policy_relevance_hu`.

2. **Expand the RePEc-ID coverage.** Only 13 of 82 authors have
   `repec_id`. The RePEc-scraping workflow (`scripts/verify_repec.py` +
   ad-hoc scraping in `scripts/apply_repec_ids.py`) needs IDs to run.
   Each new ID also unlocks:
   - catching missing published papers RePEc knows about but OpenAlex
     misses (especially KSz Hungarian articles);
   - finding 2023+ unpublished CEPR DPs (we already found 4 this way).

3. **Author-bio verification.** Many new-author bios are
   `review_status: needs-verification` or `stub`. The landing page now
   CTAs authors to check their own info. Incoming submissions should land
   in `submissions/` — see `submissions/README.md` for the processing
   flow (`author-submission-workflow` in `CLAUDE.md`).

### Known data-quality issues

- **Low-paper-count authors** (may indicate OpenAlex cluster problem or
  editor-override inclusion):
  - barath-lajos: 2, hajdu-gabor: 2, keller-tamas: 2, benedek-marton: 3,
    csercsik-david: 3, sandor-laszlo: 2, reizer-balazs: 10 (cluster issue
    documented, see `CLAUDE.md`).
  - Cross-check with their personal websites / RePEc when a `repec_id` is
    added.

- **Author-string vs author-id mismatch.** When new authors are admitted,
  pre-existing papers sometimes still carry the plain-string form of the
  name. One-off fix pass done for `elek-peter` and ~35 others; if
  discrepancies return, the pattern from commit `2acf29f` works.

### Draft-pipeline hygiene

- `ANTHROPIC_API_KEY` is unset in this env. The current drafting has
  been done by Claude Code reading abstracts inline. If the user wants
  to run `scripts/ingest.py draft` (the automated pipeline), the key
  needs to be in env.

- Draft-quality lengths mostly fine; some policy sections are below the
  60-word floor for pure-theory papers — defensible but flag to review.

### UI/site tasks

- Landing-page banner edits (EN + HU) landed in `b9ddb58`. If the user
  wants the contribute CTA more prominent, consider a standalone card
  rather than an appended paragraph at the end of the scope-block.

- Hungarian affiliation support: `hu/author.html` now reads
  `x.name_hu || x.name` (fallback). English `author.html` uses `x.name`
  directly. Both merged with `fde54d7`.

- **Feature flag**: `scope-block` content lives in both `index.html` and
  `hu/index.html`; if they drift, remember to update both.

## Recent session summary (instructions + what was done)

| # | User input | What I did |
|---|---|---|
| 1 | "namesake cleanup → HU translations → quality review" | Deleted 13 namesake papers (Robot Chess, Archaeopress, etc.); rewired 36 string attributions; applied ~40 HU translations; fixed 23 missing-country fields. [2acf29f, e2de339, 5ad91f4, dab10f0, d7108b9] |
| 2 | Kóczy feedback (bio; kiss-koczy dup; book instead of chapter; Omega paper) | Rewrote Kóczy bio; dedup'd 15 slug-duplicates; reviewed `koczy-2018-tadls` (actually the Springer book chapter "The Shapley-value", confirmed correct); **added the Omega paper manually** — Bíró-Koczy-Gyetvai-Kiss-Kovács 2025, DOI 10.1016/j.omega.2025.103403. Fixed all author affiliations EN+HU. [500ebf1, fde54d7] |
| 3 | Add unpublished 2023+ CEPR/NBER WPs | Added 4: Békés-Koren-Hinz-Lohmann 2025 CEPR DP 21145 "Vibe Coding Kills OSS"; Békés-Bonfiglioli-Crinò-Gancia 2025 CEPR DP 20026 "Integrators and Robot Adoption"; He-Kondor-Li 2025 NBER WP 34450; Koren-Telegdy 2024 CESifo WP 11164. [1eecef6] |
| 4 | Review bios + affiliations; drop HUN-REN; find missing Hungarians abroad | Dropped HUN-REN prefix (47 files); fixed Bencsik (Vanderbilt+IZA), Wachs (Corvinus primary), Lengyel (Corvinus primary), Juhász Réka (UBC AP + NBER FRF + CEPR RA); proposed 6 strong + several borderline candidates. [33f9a4f] |
| 5 | "Add all strong candidates. Nobody other." + rewrite landing-page banner + rename "Who's on this page" | Added 6 authors (Bárány, Vonyó, Temesváry, Szerb, Simonovits, Darvas) — full pipeline run. Replaced the beta-banner with a contribute-oriented paragraph at the bottom of the scope-block in both `index.html` and `hu/index.html`. Renamed H2 to "Who and What is on the list" / "Kiket és miket találsz itt". [b9ddb58] |
| 6 | "Bárány is at CEU, Vonyó is at Bocconi. Szerb and Kézdi passed away." + create handover | Applied the 4 corrections (uncommitted; see Uncommitted changes above). Wrote this handover. |

## Open questions (user-flagged, not yet decided)

- **Szerb / Kézdi death years.** Guessed 2024 (Szerb) and 2023 (Kézdi).
  Both marked `bio_review: needs-verification` until the user confirms
  exact dates.

- **Is Szerb László's RePEc ID `psz66`?** I picked that from the RePEc
  top-Hungary list. Worth confirming if the user has his personal RePEc
  profile URL.

- **Bárány Zsófia at CEU** — what is the exact title? I used "Associate
  Professor of Economics". Could be Assistant. User may want to verify.

- **Vonyó Tamás at Bocconi** — title? I used "Associate Professor of
  Economic History". Confirm.

## Session-specific context for a fresh session

- **Author-JSON schema quirk:** affiliations are now
  `{name, name_hu, role, role_hu, start}`. `hu/author.html` reads
  `name_hu` with fallback to `name`. Don't drop `name_hu` when editing.

- **"KRTK" without HUN-REN prefix** is the canonical form now (dropped
  47× in `33f9a4f`). If a future operation writes "HUN-REN KRTK", the
  same find-replace pattern applies.

- **WP rule**: Only 2023+ papers in CEPR / NBER / IZA / CESifo survive
  the whitelist as working papers. Everything else must be a journal
  article on `data/journals.json` (whitelist), a book chapter, or gets
  dropped.

- **Journal whitelist** at `data/journals.json` currently has 185
  entries. `scripts/apply_journal_whitelist.py` is the enforcer and
  contains the alias table. Hungarian journals explicitly allowed:
  Közgazdasági Szemle, Szigma, Külgazdaság.

- **Topic list at 19** (was 15): added `behavioral`,
  `industrial-organization`, `macroeconomics`, `agriculture-food`.
  Vocab in `scripts/ingest.py` and `scripts/apply_drafts.py` matches.

- **Generic-science journals** (Nature, Science, PNAS, Nature Comms,
  Nature Human Behaviour, Science Advances, PNAS) are on the whitelist
  but catch namesakes easily. Spot-checked and cleaned in commit
  `fde0380`, `2acf29f`. If new discovery adds more, watch for
  archaeogenetics / particle physics / rainforest ecology.

- **Email-collection policy**: emails live in `not-shared/contacts.json`
  (gitignored). Never display on the site.

## Next step (if the user just says "continue")

1. Commit the 4 pending author fixes:
   `git add data/authors/{barany-zsofia,vonyo-tamas,szerb-laszlo,kezdi-gabor}.json && git commit && git push`
2. Ask the user: do they have exact death years for Szerb and Kézdi?
3. Suggest spending tokens on one of:
   (a) drafting the remaining ~400 `metadata-fetched` papers;
   (b) adding more RePEc IDs (13 → 82 is the big gap);
   (c) Hungarian translations for the ~170 drafted-but-HU-missing papers;
   (d) another author-bio verification pass (all the KRTK seed authors
       created in the July 2024 mass onboarding still need their bios
       verified).
